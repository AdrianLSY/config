confirm_os_window_close 0

# Mac-style Cmd+C / Cmd+V (routed through keyd as Ctrl+C / Ctrl+V)
# copy_or_interrupt: copy if selection exists, else send SIGINT — preserves normal Ctrl+C behaviour
map ctrl+c copy_or_interrupt
map ctrl+v paste_from_clipboard
